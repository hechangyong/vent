<mapper namespace="com.im.vent.bean.BaiduTokenMapper" >
    <resultMap id="BaseResultMap" type="com.im.vent.bean.BaiduToken" >
        <id column="id" property="id" jdbcType="BIGINT" />
        <result column="token" property="userName" jdbcType="VARCHAR" />
        <result column="session_key" property="passWord" jdbcType="VARCHAR" />
        <result column="scope" property="userSex" javaType="com.neo.enums.UserSexEnum"/>
        <result column="refresh_token" property="userSex" javaType="com.neo.enums.UserSexEnum"/>
        <result column="session_secret" property="userSex" javaType="com.neo.enums.UserSexEnum"/>
        <result column="expires_in" property="userSex" javaType="com.neo.enums.UserSexEnum"/>
        <result column="date" property="nickName" jdbcType="TIMESTAMP" />
    </resultMap>

    <sql id="Base_Column_List" >
        id, userName, passWord, user_sex, nick_name
    </sql>

    <select id="getAll" resultMap="BaseResultMap"  >
        SELECT
        <include refid="Base_Column_List" />
        FROM users
    </select>

    <select id="getOne" parameterType="java.lang.Long" resultMap="BaseResultMap" >
        SELECT
        <include refid="Base_Column_List" />
        FROM users
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.neo.entity.UserEntity" >
        INSERT INTO
        users
        (userName,passWord,user_sex)
        VALUES
        (#{userName}, #{passWord}, #{userSex})
    </insert>

    <update id="update" parameterType="com.neo.entity.UserEntity" >
        UPDATE
        users
        SET
        <if test="userName != null">userName = #{userName},</if>
        <if test="passWord != null">passWord = #{passWord},</if>
        nick_name = #{nickName}
        WHERE
        id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Long" >
        DELETE FROM
        users
        WHERE
        id =#{id}
    </delete>
</mapper>